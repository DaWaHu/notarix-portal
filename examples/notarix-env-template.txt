# Notarix Signings environment template
# Copy the names you need into .env.local, then enter real values locally.
# This template mirrors the supported Notarix Signings environment schema.
# .env.local may contain a working subset, but should not contain duplicate or
# unsupported keys.
# Do not commit .env.local.

# Core application
APP_URL=
SITE_LOCKED=false
AUTH_SECRET=
AUTH_TRUST_HOST=true

# Cognito identity provider / future cutover
# Add values only after the Cognito and Google Workspace configuration exists.
# Keep existing authentication as rollback until production cutover is approved.
NOTARIX_AUTH_PROVIDER=legacy
NOTARIX_AUTH_MODE=legacy
NOTARIX_OWNER_SUPER_ADMIN_EMAIL=owner@dawahucollective.com
NOTARIX_COGNITO_REGION=us-east-1
NOTARIX_COGNITO_USER_POOL_ID=us-east-1_HL3XdEIQ9
NOTARIX_COGNITO_USER_POOL_DOMAIN=
NOTARIX_COGNITO_CLIENT_ID=4hbsqt0rqt7onqp1nk8qcu7k3l
NOTARIX_COGNITO_CLIENT_SECRET=
NOTARIX_COGNITO_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_HL3XdEIQ9
NOTARIX_COGNITO_JWKS_URL=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_HL3XdEIQ9/.well-known/jwks.json
NOTARIX_COGNITO_REDIRECT_URI=http://localhost:3000/auth/callback
NOTARIX_COGNITO_LOGOUT_URI=http://localhost:3000/
NOTARIX_COGNITO_STAFF_IDP_NAME=GoogleWorkspace
NOTARIX_COGNITO_ALLOWED_STAFF_DOMAIN=dawahucollective.com
NOTARIX_SESSION_COOKIE_SECRET=
NOTARIX_SESSION_COOKIE_NAME=

# Seed owner bootstrap
SEED_OWNER_EMAIL=
SEED_OWNER_PASSWORD=
SEED_OWNER_FIRST_NAME=
SEED_OWNER_LAST_NAME=

# Database
DATABASE_URL=
# Runtime only. Production uses AWS RDS through a Production-scoped Vercel value.
# Preview must use a branch-specific Neon pooled URL with sslmode=verify-full.
NOTARIX_DATABASE_ENVIRONMENT=
NOTARIX_DATABASE_PROVIDER=
NOTARIX_DATABASE_RESOURCE_ID=
NOTARIX_DATABASE_ENDPOINT_ID=
NOTARIX_DATABASE_NAME=
NOTARIX_DATABASE_ROLE_CLASS=runtime
NOTARIX_PRODUCTION_DATABASE_HOST_SHA256=

# Migration/operator process only. Never add this variable to Vercel application
# runtime, never fall back to DATABASE_URL, and never commit a populated value.
DATABASE_MIGRATION_URL=

# Storage / evidence / order documents
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
NOTARIX_STORAGE_REGION=
NOTARIX_STORAGE_BUCKET=
NOTARIX_STORAGE_ENDPOINT=
NOTARIX_STORAGE_WEBHOOK_SECRET=

# Evidence malware scanning callbacks
NOTARIX_MALWARE_WEBHOOK_SECRET=

# Shared evidence callback verification secret, if used for storage and malware
NOTARIX_EVIDENCE_WEBHOOK_SECRET=

# Email notifications
NOTARIX_EMAIL_WEBHOOK_SECRET=
AWS_SES_REGION=
AWS_SES_FROM_EMAIL=
AWS_SES_TO_EMAIL=

# SMS / phone notifications
AWS_SMS_REGION=
AWS_PINPOINT_APPLICATION_ID=
AWS_SNS_ORIGINATION_NUMBER=
NOTARIX_SMS_WEBHOOK_SECRET=

# Shared callback verification secret, if used for both email and SMS
NOTARIX_NOTIFICATION_WEBHOOK_SECRET=
